pipeline {
    agent any
    stages {
        stage ("code") {
            steps {
                echo "execute code"
            }
        }
        stage ("build") {
            steps {
                echo "execute build"
            }
        }
    }
}

